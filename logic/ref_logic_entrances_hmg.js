/**
 * ref_logic_entrances_hmg.js — Hybrid Major Glitches entrance logic.
 *
 * Upstream this file is byte-identical to the Overworld Glitches one, so it is
 * an alias rather than a second 30KB copy. Load order matters: the OWG file
 * has to come first. If the two ever diverge upstream, replace this with a
 * real copy renamed the same way.
 */
window.logic_entrances_hmg = window.logic_entrances_owg;
